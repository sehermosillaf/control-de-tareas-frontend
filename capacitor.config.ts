import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'control_de_tareas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
